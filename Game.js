var array = []
var num1, num2
var count = 1

function printResult(num) {
    //if staik
    if (num === 10) {
        //wait until next one
    } else {

        num1 = num

    }
}

function secondtry(num) {
    num2 = num
    var sum = num1 + num2
    if (sum === 10) {
        //wait next result
    } else {
        console.log(sum)
    }

}
printResult(5)